## HW29 – Jenkins + WebdriverIO

This project demonstrates running WebdriverIO tests via Jenkins.

### Jenkins build steps:
- Clone repository
- Install dependencies via `npm ci`
- Run tests via `npx wdio run wdio.conf.ts`

### Result:
- Tests executed successfully
- Dot and Spec reporters visible in Jenkins console output
